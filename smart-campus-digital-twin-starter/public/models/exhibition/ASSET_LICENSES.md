# Exhibition demo asset notices

The following local demo assets were copied from `@pmndrs/assets` version 1.7.0:

- `stanford-bunny.glb`
- `suzanne.glb`
- `../../environments/night-gallery.exr`

The package declares the CC0-1.0 license. These files are included only as lightweight, replaceable learning-purpose samples.

The sixteen files under `curated-demo/` were procedurally generated for this project by `scripts/generate-curated-demo-assets.py` and are released as CC0-1.0 learning assets. Their titles, tags, credits and byte sizes are stored in `curated-demo/manifest.json`.

The PNG files under `public/artworks/` and the MP4/JPEG files under `public/media/exhibition/` were generated specifically for this project. They can be replaced by user-owned artwork and media.

Always preserve the license and credit metadata for models imported through the asset synchronization pipeline.
