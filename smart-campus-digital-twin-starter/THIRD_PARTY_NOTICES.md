# Third-Party Notices

This project uses open-source JavaScript packages listed in `package.json` and locked in `package-lock.json`. Each package retains its own license.

## Demonstration media assets

`public/models/exhibition/stanford-bunny.glb`, `public/models/exhibition/suzanne.glb`, and `public/environments/night-gallery.exr` were sourced from `@pmndrs/assets` version 1.7.0. The package declares the CC0-1.0 license.

The Draco decoder files in `public/draco/` are distributed as runtime decoder assets used by Three.js GLTFLoader. Their upstream licensing is retained by the Three.js project and Google Draco.

The two PNG files under `public/artworks/` are project-generated concept images used as replaceable digital-art examples.
